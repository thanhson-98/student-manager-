package com.example.student_manager.repository;

import com.example.student_manager.entity.AptechClass;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AptechClassRepository extends JpaRepository<AptechClass, Long> {
}
